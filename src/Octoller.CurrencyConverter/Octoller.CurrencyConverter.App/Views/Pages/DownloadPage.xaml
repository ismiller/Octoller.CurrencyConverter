<Page
    x:Class="Octoller.CurrencyConverter.App.Views.Pages.DownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
    xmlns:local="using:Octoller.CurrencyConverter.App.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Octoller.CurrencyConverter.App.ViewModels"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity" 
    xmlns:core="using:Microsoft.Xaml.Interactions.Core" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding DownloadView, Source={StaticResource sLocator}}"
    Height="150" Width="500">

    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:EventTriggerBehavior.Actions>
                <core:InvokeCommandAction Command="{Binding Load}" CommandParameter="{Binding CurrentFrame}" />
            </core:EventTriggerBehavior.Actions>
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>

    <Grid>
        <ProgressRing x:Name="progressingBar" 
                      Width="100" Height="100" 
                      Visibility="Visible" 
                      IsActive="{Binding IsActive}">
            
        </ProgressRing>
    </Grid>
</Page>
