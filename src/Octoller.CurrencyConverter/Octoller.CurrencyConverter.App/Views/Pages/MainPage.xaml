<Page
    x:Class="Octoller.CurrencyConverter.App.Views.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
    xmlns:local="using:Octoller.CurrencyConverter.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Octoller.CurrencyConverter.App.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding MainView, Source={StaticResource sLocator}}"
    Width="500" Height="150">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Content="RUB"></Button>
        <Button Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" Content="USD"></Button>
        <TextBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" 
                 VerticalAlignment="Center" Height="34" MaxLength="40" Width="400"></TextBox>
        <TextBox Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" 
                 VerticalAlignment="Center" Height="34" MaxLength="40" Width="400"></TextBox>
    </Grid>
</Page>
